# hex-row-reduce

Executable row reduction (RREF over fields) plus span / nullspace machinery over
the `hex-matrix` dense core. Mathlib-free.

This repository is published from the `hex-dev` monorepo; do not edit it by hand.
See `SPEC/hex-row-reduce.md`.
